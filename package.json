{
  "name": "news",
  "private": true,
  "scripts": {
    "start": "prpl-server --root build/ --host 0.0.0.0 --https-redirect",
    "deploy": "polymer build && gcloud app deploy --project polymer-news"
  },
  "engines": {
    "node": ">=6.0.0"
  },
  "dependencies": {
    "prpl-server": "^0.10.1"
  }
 }
